syntax = "proto3";

package awrfo.contracts.commands.v1;

import "buf/validate/validate.proto";
import "awrfo/contracts/schemas/common/v1/coordinate.proto";
import "awrfo/contracts/commands/v1/command_type.proto";
import "awrfo/contracts/commands/v1/command_policy.proto";

message ActionCommand {
  string id = 1 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.uuid = true
  ];

  uint64 ts_ms = 2 [(buf.validate.field).required = true];

  string robot_id = 3 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.uuid = true
  ];

  awrfo.contracts.commands.v1.CommandType type = 4 [
    (buf.validate.field).required = true
  ];

  awrfo.contracts.commands.v1.CommandPolicy policy = 5 [
    (buf.validate.field).required = true
  ];

  option (buf.validate.message).oneof = {
    fields: [ "assign_task", "cancel_task", "move_to", "wait" ],
    required: true
  };

  AssignTask assign_task = 6;
  CancelTask cancel_task = 7;
  MoveTo move_to = 8;
  Wait wait = 9;
}

message AssignTask {
  string task_id = 1 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.uuid = true
  ];
}

message CancelTask {
  string task_id = 1 [
    (buf.validate.field).required = true,
    (buf.validate.field).string.uuid = true
  ];

  optional string reason = 2 [(buf.validate.field).required = false];
}

message MoveTo {
  // Target pose in map frame (v1: x,y only).
  awrfo.contracts.schemas.common.v1.Coordinate target = 1;

  // Optional tolerance for "arrived".
  optional float arrive_eps = 3 [
    (buf.validate.field).required = false,
    (buf.validate.field).float.gte = 0.0
  ];
}

message Wait {
  // Duration to wait.
  uint64 duration_ms = 1;
}
